<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">
    <fragment
        android:id="@+id/homeFragment"
        android:name="fullstack.application.spherebeat.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home">
        <deepLink
            android:id="@+id/deepLinkToHome"
            app:uri="app://fullstack.application.spherebeat/home" />
    </fragment>
    <fragment
        android:id="@+id/listsFragment"
        android:name="fullstack.application.spherebeat.ListsFragment"
        android:label="Lists"
        tools:layout="@layout/fragment_lists" />
    <fragment
        android:id="@+id/searchFragment"
        android:name="fullstack.application.spherebeat.SearchFragment"
        android:label="Search"
        tools:layout="@layout/fragment_search" />
    <fragment
        android:id="@+id/profileFragment"
        android:name="fullstack.application.spherebeat.ProfileFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_profile" />

</navigation>